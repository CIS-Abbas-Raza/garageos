import { EntityCrudPage } from '@/components/dashboard/entity-crud-page'
export default function QuotationsPage() { return <EntityCrudPage config={{ resource: 'estimations', title: 'Quotations', description: 'Prepare estimates, track approval, and turn accepted work into jobs.', fields: [{ key: 'description', label: 'Description', required: true }, { key: 'customerId', label: 'Customer ID', required: true }, { key: 'vehicleId', label: 'Vehicle ID', required: true }, { key: 'total', label: 'Total', type: 'number', required: true }, { key: 'status', label: 'Status', type: 'select', options: ['draft', 'sent', 'accepted', 'rejected'], required: true }], columns: ['description', 'customerId', 'total', 'status'], empty: 'No quotations yet.' }} /> }
