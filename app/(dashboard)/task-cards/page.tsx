import { EntityCrudPage } from '@/components/dashboard/entity-crud-page'
export default function TaskCardsPage() { return <EntityCrudPage config={{ resource: 'jobCards', title: 'Task Cards', description: 'Track work from assignment through completion with mechanic ownership.', fields: [{ key: 'title', label: 'Task title', required: true }, { key: 'customerId', label: 'Customer ID', required: true }, { key: 'vehicleId', label: 'Vehicle ID', required: true }, { key: 'mechanicId', label: 'Mechanic ID' }, { key: 'status', label: 'Status', type: 'select', options: ['pending', 'in-progress', 'completed', 'on-hold'], required: true }, { key: 'priority', label: 'Priority', type: 'select', options: ['low', 'medium', 'high'], required: true }], columns: ['title', 'customerId', 'mechanicId', 'status', 'priority'], empty: 'No task cards yet.' }} /> }
