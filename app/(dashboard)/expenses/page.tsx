import { EntityCrudPage } from '@/components/dashboard/entity-crud-page'
export default function ExpensesPage() { return <EntityCrudPage config={{ resource: 'expenses', title: 'Expenses', description: 'Track daily operating costs and understand where money goes.', fields: [{ key: 'date', label: 'Date', type: 'date', required: true }, { key: 'category', label: 'Category', required: true }, { key: 'description', label: 'Description', required: true }, { key: 'amount', label: 'Amount', type: 'number', required: true }], columns: ['date', 'category', 'description', 'amount'], empty: 'No expenses recorded.' }} /> }
